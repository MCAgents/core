name: MCAgents
version: '${version}'
main: io.github.mcagents.core.foliamc.FoliaMCCorePlugin
api-version: '1.21'
description: The MCAgents agent API. Provides language model access to other plugins.
authors: [jetsadawijit]
website: https://mcagents.github.io/website

# The provider performs no scheduling and touches no game state: every call is
# a CompletableFuture over an HTTP client. There is nothing here for regionised
# multithreading to break, which is what makes this claim honest.
folia-supported: true
# The only command is administrative. This plugin is a service that consumer
# plugins bind to; it exposes nothing to players.
commands:
  agents:
    description: Manage MCAgents API tokens and see which platforms have a usable one.
    usage: /agents | /agents reload | /agents <platform> token <add|remove> <token>

permissions:
  mcagents.admin:
    description: Allows running /agents, which manages API tokens and reports credential health.
    default: op
